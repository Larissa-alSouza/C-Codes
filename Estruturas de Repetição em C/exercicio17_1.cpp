/* 17.1- Use um loop for ou while para imprimir os números de 1 a 20, um por linha. */

#include <stdio.h>

int main() {
    
    int num = 0;
    
    while(num < 21) {
        printf("%d \n", num);
        
        num++;
    }

    return 0;
}
